#include <iostream>
#include <stdlib.h>
using namespace std;
int main()
{
	
	int i;
	int array[100];
	for (int &i :array) {
		i = rand() % 100;
		cout << i << endl;
		
	}
	int max = array[0]; 
		for (int &i : array) {
			if (max < i) {
				max = array[i];
			}
	}
		cout <<"최댓값" << max;
}

	
